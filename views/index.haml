#wrapper
  #line{ style: "width: 48px"}
    %h1= "KRISTINA BUTKUTE"
#boxes_wrap
  #boxes{ style: "display: none" }
    - dirs.each_with_index do |dir, idx|
      %div{ :"data-href" => "/pages/#{dir}", style: "background: url('/images/_menu/#{dir}.jpg')", class: "#{"mini" if idx == 9}" }
        %span= dir.capitalize.humanize 
        
        